spring:
  autoconfigure:
    exclude: io.sentry.spring.boot.jakarta.SentryAutoConfiguration

sentry:
  enabled: false
  dsn:

app:
  mqtt:
    client-id: device-service-test
  scheduler:
    poll-interval: 60000
